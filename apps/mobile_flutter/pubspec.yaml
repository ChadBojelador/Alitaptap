name: alitaptap_mobile
description: ALITAPTAP — community problems to student research
publish_to: "none"
version: 0.2.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^8.0.2
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  # cloud_firestore: ^6.x — still bypassed, see bypass docs
  maplibre_gl: ^0.25.0
  geolocator: ^14.0.2
  http: ^1.2.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
