[phases.setup]
nixPkgs = ["nodejs_22", "python311"]

[phases.install]
cmds = [
  "npm install --prefix server",
  "pip install -r server/credibility_checker/requirements.txt"
]

[start]
cmd = "cd server && node server.js"