{
  "name": "ithink",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install && pip3 install -r credibility_checker/requirements.txt",
    "start": "cd server && node server.js",
    "dev:server": "cd server && node server.js",
    "dev:client": "cd client && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
